<%- include ('partials/header.ejs') %>
    <%- include ('partials/navbar.ejs') %>

        <div class="container">
            <div class="card">
                <div class="card-header">
                    <h1>Voluntarios</h1>
                </div>

                <div class="card-body">
                    <!-- Formulário de inclusão -->
                    <form action="/voluntarios/new" method="POST">
                        <div class="mb-3">
                            <label>Nome do voluntario:</label>
                            <input type="text" name="nome" placeholder="Insira o nome do voluntario"
                                class="form-control" required>
                        </div>

                        <div class="mb-3">
                            <label>CPF do voluntario:</label>
                            <input type="text" name="cpf" placeholder="Insira o email do voluntario"
                                class="form-control" required>
                        </div>

                        <div class="mb-3">
                            <label>Endereço do voluntario:</label>
                            <input type="text" name="endereco" placeholder="Insira o endereço do voluntario"
                                class="form-control" required>
                        </div>

                        <div class="mb-3">
                            <input type="submit" class="btn btn-success" value="Cadastrar">
                            <input type="reset" class="btn btn-danger" value="Limpar">
                        </div>
                    </form>
                </div>

                <div class="card-body">
                    <table>
                        <thead>
                            <tr>
                                <th></th>
                                <th>Nome:</th>
                                <th>CPF:</th>
                                <th>Endereço:</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% voluntarios.forEach(voluntario=> { %>
                                <tr>
                                    <td><img src="imgs/avatar.png" width="30px"></td>
                                    <td>
                                        <%= voluntario.nome %>
                                    </td>
                                    <td>
                                        <%= voluntario.cpf %>
                                    </td>
                                    <td>
                                        <%= voluntario.endereco %>
                                    </td>
                                    <td>
                                        <a href="/voluntarios/edit/<%= voluntario.id %>">
                                            <button class="btn btn-warning">
                                                Editar
                                            </button>
                                        </a>
                                    </td>
                                    <td>
                                        <form method="POST" action="/voluntarios/delete/<%= voluntario.id %>">
                                            <input type="hidden" name="id" value="<%= voluntario.id %>">
                                            <button type="submit" class="btn btn-danger"
                                                onclick="return confirm('Deseja mesmo excluir a voluntario <%= voluntario.nome %>?')">
                                                Excluir
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <%- include ('partials/footer.ejs') %>